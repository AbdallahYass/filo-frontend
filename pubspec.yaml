name: filo_menu
description: "Filo Menu: The Modern Restaurant App."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  
  # 🔥 التعديل الأول: مكتبات التدويل (Localization)
  flutter_localizations: 
    sdk: flutter
  intl: any
  
  # بقية المكتبات
  cupertino_icons: ^1.0.8
  mobile_scanner: ^5.1.0 
  qr_flutter: ^4.1.0 
  http: ^1.2.1
  video_player: ^2.8.1
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.5.3
  google_sign_in: ^6.1.0
  intl_phone_field: ^3.2.0
  connectivity_plus: ^5.0.2
  geolocator: ^11.0.0
  provider: ^6.1.2
  flutter_gen: ^5.12.0
  pinput: ^4.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  
  # حزم تخصيص الهوية البصرية لـ Filo Menu
  flutter_launcher_icons: "^0.13.1"
  flutter_native_splash: "^2.3.1"

# إعدادات حزمة الأيقونات (Flutter Launcher Icons)
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icons/filo.png"
  min_sdk_android: 21
  web:
    generate: true
    image_path: "assets/icons/filo.png"
    background_color: "#hex_code"
    theme_color: "#hex_code"
    
# إعدادات حزمة شاشة البداية (Flutter Native Splash)
flutter_native_splash:
  background_image: "assets/icons/splash_bg.png"
  color: "#1A1A1A"
  fullscreen: true
  android_gravity: fill
  ios_content_mode: cover
  android_12:
    color: "#1A1A1A"

# 🔥 التعديل الثاني: تفعيل التوليد التلقائي للغات (مهم جداً)
flutter:
  generate: true 
  
  uses-material-design: true

  # انتبه: كلمتي assets تحت بعضهما تماماً
  assets:
    - assets\icons\filo.png
    - assets\icons\splash_bg.png
    - assets\videos\intro.mp4