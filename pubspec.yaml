name: filo_menu
description: "Filo Menu: The Modern Restaurant App."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  mobile_scanner: ^5.1.0  # 👈 أضف هذه
  qr_flutter: ^4.1.0      # 👈 أضف هذه المكتبة الجديدة
  # حزمة للتواصل مع السيرفر (لربط API لاحقاً)
  http: ^1.2.1
  video_player: ^2.8.1
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.5.3
  google_sign_in: ^6.1.0
  intl_phone_field: ^3.2.0
  connectivity_plus: ^5.0.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  
  # حزم تخصيص الهوية البصرية لـ Filo Menu
  flutter_launcher_icons: "^0.13.1"
  flutter_native_splash: "^2.3.1"

# إعدادات حزمة الأيقونات (Flutter Launcher Icons)
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icons/filo.png"
  min_sdk_android: 21
  web:
    generate: true
    image_path: "assets/icons/filo.png"
    background_color: "#hex_code"
    theme_color: "#hex_code"
    
# إعدادات حزمة شاشة البداية (Flutter Native Splash)
flutter_native_splash:
  # الصورة التي تريدها كخلفية
  background_image: "assets/icons/splash_bg.png"
  
  # لون الخلفية (احتياطي)
  color: "#1A1A1A"

  # 1. إخفاء شريط الحالة (الساعة والبطارية) لتصبح الشاشة كاملة
  fullscreen: true

  # 2. إعدادات أندرويد (لجعل الصورة تمط وتملأ الشاشة)
  android_gravity: fill
  
  # 3. إعدادات آيفون (تغطية كاملة مع قص الزوائد)
  ios_content_mode: cover
  
  # 4. (مهم جداً) إعدادات أندرويد 12 فما فوق
  # أندرويد 12 يمنع صور الخلفية الكاملة في السبلاش سكرين افتراضياً
  # هذا يحاول جعلها أقرب ما يمكن، لكنه سيظهر الأيقونة في الوسط
  android_12:
    color: "#1A1A1A"
    # يفضل استخدام أيقونة صغيرة هنا للأندرويد 12، أو تركها تأخذ الافتراضي
    # image: "assets/icons/app_icon.png"

flutter:
  uses-material-design: true

  # انتبه: كلمتي assets تحت بعضهما تماماً
  assets:
    - assets\icons\filo.png
    - assets\icons\splash_bg.png
    - assets\videos\intro.mp4